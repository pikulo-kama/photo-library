import { createTheme } from "@mui/material";


export const theme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#ffab91',
    },
    secondary: {
      main: '#ff7043',
    },
    text: {
      primary: '#fbf6f6',
      secondary: '#9e9e9e',
      disabled: '#4e3452',
    }
  }
});
